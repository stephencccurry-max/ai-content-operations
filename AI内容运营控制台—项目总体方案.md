# AI 内容运营控制台—项目总体方案

> 文档版本：V1.1  
> 适用阶段：MVP 立项、架构确认与开发启动  
> 部署前提：个人 Windows 电脑，本地运行 Web 控制台与核心服务，调用外部大模型 API / MCP  
> 核心技术栈：Next.js + FastAPI + PostgreSQL + n8n + Docker Desktop

## 1. 执行摘要

本项目建设一套供个人日常使用的本地 AI 内容运营控制台。用户通过浏览器完成选题录入、资料调研、内容生成、质量检查、人工审核、导出或受控发布，而不需要在日常运营时直接操作 Cursor、n8n、数据库或命令行。

项目的核心价值不是再造一个“AI 写作工具”，也不是从零实现各平台的浏览器自动化，而是建立一条可追踪、可审核、可重试、可扩展的内容生产主干：

```text
任务输入 → 资料调研 → 内容策划 → 平台内容生成 → 自动质检
        → 人工审核 → 导出/受控发布 → 结果记录
```

V1 以验证内容质量、节省人工时间和跑通个人工作闭环为目标。系统采用模块化单体，而非微服务；PostgreSQL 是业务事实来源，n8n 负责编排耗时流程，FastAPI 负责所有业务规则和对外接口，Next.js 是唯一日常操作入口。外部模型、搜索服务、MCP 与平台发布工具均通过适配器隔离。

推进方式上，V1 采取「先跑通、再补齐」的节奏：M1 先用全 Mock 打通一条不依赖任何外部 API 的端到端闭环，拿到一个可本地演示的版本，随后由 M2 至 M4 逐步接入真实内容生产、质检与人工审核、稳定性与试运行。里程碑定义见 §16。

## 2. 原方案 Review 与优化结论

### 2.1 保留的正确设计

- Web 控制台是日常运营入口，Cursor 只用于开发维护，n8n 只用于流程编排与排错。
- Web 前端不直接调用 n8n、模型 API、MCP 或发布工具，统一经过 FastAPI。
- PostgreSQL 保存业务状态，n8n execution 只保存编排运行信息。
- 内部关键产物采用结构化 Schema，并保留 Prompt、模型、成本、来源和版本信息。
- 自动质检之后必须有人审，公开发布默认不得绕过人工批准。
- 发布层采用 Adapter，优先复用成熟开源组件，不从零开发登录与上传能力。
- 调研结果保留 Claim → Evidence → Source 关系，避免把模型推测当作事实。

### 2.2 需要调整的设计

| 原方案倾向 | 调整后设计 | 原因 |
|---|---|---|
| V1 同时覆盖选题、图文、视频、自动发布、分析闭环 | V1 聚焦文本主链路，图片只做引用/附件，视频仅输出结构化脚本；发布为可选受控能力 | 先验证内容是否可用，降低系统和账号风险 |
| 拆分多个独立业务服务 | 采用模块化单体 FastAPI，n8n 调用内部 API | 个人 Windows 电脑资源有限，微服务增加部署与排错成本 |
| 建设完整素材库、账号中心、数据分析中心 | V1 只做任务附件、发布配置和基础运行统计 | 避免建设尚无数据支撑的后台模块 |
| 一开始使用 SSE/WebSocket | V1 使用 3–5 秒轮询；后续需要时升级 SSE | 实现简单，足以满足个人并发量 |
| 多层 Agent 自主决策与自动循环 | 固定工作流 + Schema + 最多一次自动修订 | 可控、可测，避免成本和状态失控 |
| 统一生成长文后再截短 | 根据 Research Brief 和 Content Brief 分别生成平台版本 | 平台内容不是长文压缩，需独立适配 |
| API Key 在控制台自由编辑并存库 | V1 使用本地 `.env`/Docker Secret；页面仅显示配置状态 | 降低凭据泄露风险 |
| V1 直接依赖 MCP 完成核心业务 | MCP 仅作为外部能力适配方式之一，核心领域不依赖其协议 | MCP 服务稳定性和接口差异不应污染业务模型 |
| 一开始建设 RAG/向量数据库 | 暂不建设；以 PostgreSQL、来源记录和全文字段满足 V1 | 当前数据量与检索需求不支持该复杂度 |

### 2.3 V1 的产品原则

1. **人工掌控**：生成不等于可发布，批准是独立动作。
2. **证据优先**：涉及事实、数字和外部判断时必须可回溯来源。
3. **失败可见**：任何失败都要落库、可解释、可从安全步骤重试。
4. **单机优先**：面向一名用户、少量并发，不为未来 SaaS 提前付出复杂度。
5. **接口隔离**：模型、搜索、MCP、发布平台均可替换。
6. **渐进自动化**：先建议与生成，再审核与导出，最后才扩大自动发布。
7. **快速见效优先**：先得到端到端可运行的版本，再逐步补完备性；宁可范围小，不要长期处于不可运行状态。

## 3. 项目背景与问题定义

个人运营小红书、抖音等内容账号时，主要成本集中在：持续找选题、检索资料、组织观点、适配平台格式、反复修改、安排发布和记录结果。通用聊天工具可以生成单次文案，却通常缺少任务状态、来源证据、版本管理、审核门禁、失败补偿和跨平台复用。

因此需要的不是聊天窗口，而是一套业务系统：把分散的 AI 能力、外部搜索、MCP 与发布工具组织成稳定流水线，并让用户在 Web 控制台中掌握关键决策。

## 4. 项目目标与成功标准

### 4.1 业务目标

- 将一条内容从需求输入到“可审核稿”的人工处理时间明显缩短。
- 同一份调研资产可生成小红书图文稿和抖音口播脚本。
- 每条公开内容都有明确来源、版本、质检结果和审批记录。
- 外部能力失败时，不丢任务，不需要从头重做。
- 为后续图片、视频、自动发布和效果分析留下清晰扩展点。

### 4.2 V1 验收口径

V1 的量化验收指标（阈值、统计单位、样本量与测量方式）以《AI 内容运营控制台—V1 产品与实施设计》§18 为唯一来源，本文件不再复述具体数字，避免两份文档出现互相冲突的口径。本节只保留业务性质的定性目标：

- 任务创建是可靠动作：重复提交不产生重复任务，创建失败必须给出可理解的原因。
- 在外部服务可用的前提下，任务能够自动走到待审核状态，而不需要人工介入编排。
- 任何失败都必须在页面上显示失败步骤、原因和可执行操作，不出现「卡住但看不出为什么」的任务。
- 发布任务只能关联已批准的内容版本，不存在绕过审批的发布路径。
- 事实型核心观点可回溯到来源。数字的处理方式是：生成模型必须在产物里显式输出 `factual_claims` 数组，逐条给出数值、单位、口径和来源 ID，质检只校验这个数组，不对正文做全量数字扫描。阻断范围收窄到统计、价格、日期、比例四类事实型数字；修辞性与结构性数字（如「3 个技巧」「第 2 点」）不在此约束内。
- 端到端耗时在 V1 阶段只观测、不设阈值。一条双平台任务大约需要 15–25 次模型或搜索调用，耗时主要由外部服务决定，因此在 M4 的 20 个真实选题试运行中采集真实基线后再确定阈值。
- 用户可在一次安装过程内完成首次本地启动与示例任务创建，不需要阅读源码。

## 5. 用户、角色与使用场景

### 5.1 V1 用户

唯一核心用户是内容运营者本人，同时承担选题人、编辑、审核人和发布负责人角色。V1 不建设团队协作与复杂权限。

系统内部可保留 `actor = local_admin / system` 字段，以便审计自动动作与人工动作，但不建设 RBAC。

### 5.2 核心场景

- 输入一个主题、目标人群、传播目标、语气与目标平台，生成内容任务。
- 查看调研来源、摘要、关键事实、反方观点和不确定项。
- 查看小红书稿、抖音脚本及质量报告。
- 手工编辑并保存新版本，批准某个明确版本。
- 将批准版本导出为 Markdown/JSON，或交给发布适配器。
- 查看当前步骤、成本、失败原因，并从可重试步骤继续。

## 6. 产品范围与边界

### 6.1 V1 必做

- 本地单用户 Web 控制台。
- 内容任务创建、列表、详情、取消和归档。
- 搜索/资料采集、来源去重与 Research Brief。
- Content Brief。
- 小红书图文文案和抖音短视频脚本。
- 结构校验、规则质检、模型辅助质检和一次受控修订。
- 人工编辑、版本管理、批准、驳回和修改意见。
- Markdown/JSON 导出。
- 发布适配器框架；V1 的发布能力只包含 `manual_export` 导出，以及用 Fake Publisher 验证 Adapter 契约（scheduled / succeeded / failed / unknown 四态、幂等、发布后验证与人工确认路径），不接入任何真实平台发布。
- 基础设置状态、模型使用记录、运行日志和错误处理。
- 模型调用成本记录、任务级与日级预算，以及超限熔断：软阈值告警，硬阈值将任务置为 `needs_attention` 并停止后续调用。
- Docker Compose 本地启动、数据库迁移、备份与恢复说明。

### 6.2 V1 明确不做

- 多租户、注册、计费、团队空间、复杂权限。
- 手机 App、公网访问和云端高可用。
- 向量数据库、长期记忆、自主多 Agent。
- 自研图片/视频生成与合成引擎。
- 多账号矩阵、账号养号和规避平台风控。
- 无人工确认的全自动公开发布。
- 小红书、抖音等真实平台的发布 Adapter 接入：小红书图文缺少图片能力、抖音方向不做视频合成因而没有可发布的视频资产，V1 都没有可发布的载荷，整体推迟到 V1.1。
- 完整数据仓库、增长归因和自动 Prompt 优化。
- 对平台页面自动化的稳定性作产品级承诺。

### 6.3 V1.1 / V2 候选

- 图片卡片模板与图像生成 Provider。
- 视频素材、TTS、字幕和 FFmpeg/Remotion 合成。
- 小红书、抖音真实发布 Adapter 的接入与稳定化，与图片、视频能力一并排期。
- 发布后数据采集、内容表现看板与选题反馈。
- 远程部署、登录、多用户与权限。

## 7. 需求模块

| 模块 | V1 职责 | 优先级 |
|---|---|---:|
| 仪表盘 | 待审核、运行中、失败、待发布、最近任务、近 7 日成本 | P1 |
| 内容任务 | 创建、筛选、详情、取消、归档、重试 | P0 |
| 调研与策划 | 来源、摘要、观点、证据、Content Brief | P0 |
| 内容生成 | 小红书稿、抖音脚本、结构化输出 | P0 |
| 质量与审核 | 自动 QC、人工编辑、版本、批准/驳回 | P0 |
| 发布与导出 | Markdown/JSON 导出、发布任务、结果验证 | P0/P1 |
| 运行与设置 | Provider 配置状态、Prompt 版本、运行日志 | P1 |
| 数据分析 | 任务量、耗时、成本和成功率，由仪表盘的近 7 日轻量统计承载，V1 不建独立页面 | P1（轻量） |

## 8. 端到端业务流程

```text
[创建任务]
    │ 参数校验、幂等创建
    ▼
[进入队列]
    │ n8n 拉起运行
    ▼
[调研]
    │ 查询生成 → 搜索 → 去重 → 抽取 → 来源评分
    ▼
[Research Brief]
    │ Claim / Evidence / Source / Uncertainty
    ▼
[Content Brief]
    │ 角度、Hook、核心观点、结构、CTA
    ├───────────────┐
    ▼               ▼
[小红书稿]      [抖音脚本]
    └───────┬───────┘
            ▼
         [自动 QC]
            │ 不通过且未修订过：一次定向修订
            ▼
         [待人工审核]
      ┌─────┼────────┐
      ▼     ▼        ▼
    批准   驳回    手工编辑
      │              │ 新版本后再批准
      ▼              │
 [导出/创建发布任务] ◄┘
      │
      ▼
 [执行 + 发布后验证 + 结果记录]
```

## 9. 总体架构

```text
┌─────────────────────────────────────────────────────┐
│ Windows Host                                        │
│                                                     │
│  Browser                                            │
│     │                                               │
│     ▼                                               │
│  Next.js Web Console :3000                          │
│     │ REST + polling                                │
│     ▼                                               │
│  FastAPI Control API :8000                          │
│  ├─ task / content / review / publish / config      │
│  ├─ domain rules + adapters                         │
│  └─ audit / idempotency / error mapping             │
│     │                         │                     │
│     ▼                         ▼                     │
│  PostgreSQL :5432          n8n :5678                │
│  Business source of truth    Workflow orchestration │
│                                │                    │
│                                ▼                    │
│                       FastAPI internal endpoints     │
│                                │                    │
│                 ┌──────────────┼──────────────┐     │
│                 ▼              ▼              ▼     │
│             LLM API       Search API       MCP/API  │
│                                             Publish │
└─────────────────────────────────────────────────────┘
```

### 9.1 职责边界

- **Next.js**：页面、表单、展示、编辑交互；不保存业务真相，不持有外部平台密钥。
- **FastAPI**：领域逻辑、权限边界、状态迁移、数据持久化、Provider/Adapter、审计和统一错误。
- **PostgreSQL**：任务、产物、版本、来源、审批、工作流、发布、调用记录的唯一事实来源。
- **n8n**：串联长耗时步骤、定时器、回调、条件分支与有限重试；不承载核心业务规则。
- **外部 API/MCP**：搜索、模型或发布能力；均被适配器封装，可替换、可禁用。
- **本地文件目录**：V1 仅保存导出文件和附件；数据库只保存相对路径、哈希与元数据。

### 9.2 关键架构决策

1. 采用模块化单体，不采用微服务。
2. 前后端仅使用 REST；任务进度 V1 轮询。
3. n8n 通过带共享密钥的内部 API 调用 FastAPI，不直接写核心业务表。
4. 每次外部调用设置超时、重试上限和幂等键。
5. 所有状态迁移由 FastAPI 校验；n8n 只申请迁移。
6. 内容产物不可原地覆盖，修改生成新版本。
7. 自动发布不是 V1 成功的前提，手动导出是稳定保底路径。
8. 保留 n8n 作为编排层，是一项有意的技术投资，而不是默认选择。就本项目当前的编排复杂度而言，用 FastAPI 内的任务队列同样能实现，且能省掉一个容器、一套内部 API 鉴权面和双状态源带来的对账开销。之所以仍然保留，是因为本项目同时充当 n8n 编排能力的试验场：后续其他项目以及本项目新增的功能模块都可能复用这套编排经验与工作流资产。因此我们明确接受它带来的额外部署成本、双状态源和对账工作量，实施过程中不因为「看起来多余」而中途更换架构。

## 10. 核心数据对象

| 对象 | 说明 | 关键关系 |
|---|---|---|
| Project | 内容项目/账号方向，V1 可只有一个默认项目 | 1:N ContentTask |
| ContentTask | 一次内容生产需求与总状态 | 关联运行、来源、产物、审核、发布 |
| WorkflowRun | 一次端到端运行 | 1:N WorkflowStepRun |
| WorkflowStepRun | 单个步骤状态、重试次数、错误 | 属于 WorkflowRun |
| SourceDocument | 来源 URL、标题、摘要、哈希、可信度 | N:M ResearchClaim |
| ResearchBrief | 结构化调研结论 | 属于任务且有版本 |
| ResearchClaim | 观点、证据、置信度、不确定标记 | N:M SourceDocument |
| ContentBrief | 内容角度、结构、CTA | 属于任务且有版本 |
| ContentOutput | 某平台某类型的内容版本 | 属于任务；可被批准 |
| QualityReport | 规则与模型质检结果 | 绑定明确 ContentOutput 版本 |
| ReviewDecision | 批准、驳回、修改请求与备注 | 绑定明确版本 |
| PublishJob | 某版本向某渠道的发布/导出任务 | 仅允许批准版本创建 |
| PublishAttempt | 每次执行及验证结果 | 属于 PublishJob |
| ProviderCall | 模型/搜索/MCP 调用、耗时、成本与错误 | 关联任务和步骤 |
| AuditEvent | 用户或系统动作记录 | 不可变追加 |

## 11. 开源组件复用策略

### 11.1 原则

- 复用边缘能力，不让第三方项目成为核心数据模型。
- 优先通过进程/API/Adapter 集成，避免大面积复制源码。
- 接入前检查许可证、维护活跃度、Windows/Docker 支持、凭据处理、Issue 和安全面。
- 为每个发布 Adapter 保留 `disabled` 开关和 `manual_export` 回退。
- 浏览器自动化组件必须在独立目录/容器运行，不向 Web 暴露未鉴权接口。

### 11.2 候选组件定位

| 组件 | 建议定位 | 许可证 | 最后核验日期 | V1 决策 |
|---|---|---|---|---|
| `xpzouying/xiaohongshu-mcp` | 小红书发布执行候选 | 接入前核验 | 未核验 | V1 不接入；V1.1 与图片能力一并评估 |
| `dreammis/social-auto-upload` | 抖音等多平台上传候选 | 接入前核验 | 未核验 | V1 不接入；V1.1 与视频能力一并评估 |
| `BetaStreetOmnis/xhs_ai_publisher` | Windows 登录态、预览、发布架构参考 | 接入前核验 | 未核验 | 参考，不整体 Fork |
| `YYH211/xiaohongshu` | 搜索 + MCP + 发布 PoC 参考 | 接入前核验 | 未核验 | 参考编排方式 |
| ClipForge / VideoClaw | 后续视频工厂参考 | 接入前核验 | 未核验 | V1 不引入 |
| FFmpeg / Remotion | 后续视频合成 | 接入前核验 | 未核验 | V2 候选 |

「许可证」一列刻意不预先填写具体协议名：这类面向小红书、抖音的平台自动化项目不少采用 AGPL，也有相当一部分根本没有 License 声明，属于接入前必须逐个确认并记录核验日期的事项，不能凭印象填写。

上述项目的活跃度、接口和许可证可能变化，正式接入前必须重新核验，不把历史调研数字写入架构硬约束。

## 12. 技术选型

### 12.1 推荐基线

| 层 | 选择 | 说明 |
|---|---|---|
| 前端 | Next.js + TypeScript + Tailwind CSS + shadcn/ui | 适合管理台，Cursor 易维护 |
| 数据请求 | 原生 fetch 或 TanStack Query | 统一缓存、轮询和错误态 |
| 后端 | FastAPI + Pydantic v2 | Schema-first，适合模型与异步 API |
| ORM/迁移 | SQLAlchemy 2 + Alembic | 明确迁移与事务边界 |
| 数据库 | PostgreSQL 16 | JSONB + 关系数据，替代额外向量库 |
| 工作流 | n8n | 可视化编排、定时、回调和错误工作流 |
| 资料检索 | 自带正文或高质量摘要的搜索 API（Tavily / Exa / Bocha 一类） | 直接取得可用正文文本，整层绕开自建抓取；正文来自 API 而非本地抓取，版权与留存策略据此制定 |
| HTTP Client | httpx | 异步、超时与连接池 |
| 测试 | pytest + Playwright | 后端、集成与关键 UI 流程 |
| 部署 | Docker Compose | Windows + Docker Desktop 可重复启动 |
| 日志 | 结构化 JSON 日志 + 数据库运行记录 | V1 不引入 ELK/Prometheus |

### 12.2 不建议在 V1 引入

- Kubernetes、消息队列、Redis、Kafka。
- 单独 API Gateway、Service Mesh。
- Elasticsearch、专用向量数据库。
- WebSocket、复杂事件总线。
- 自建网页抓取与正文提取管线：反爬、JS 渲染、编码识别与正文降噪的成本远超收益，V1 依赖搜索 API 直接返回的正文或摘要。
- 自建模型推理（除非已有稳定 GPU 环境且明确需要）。

## 13. Windows 本地部署方式

### 13.1 运行形态

推荐使用 Docker Desktop + WSL2 backend，由 Compose 启动：

- `web`：Next.js，映射 `localhost:3000`。
- `api`：FastAPI，映射 `localhost:8000`。
- `db`：PostgreSQL，仅本机访问。
- `n8n`：映射 `localhost:5678`，仅维护时使用。

开发阶段可将 Next.js/FastAPI 在宿主机热更新运行，PostgreSQL/n8n 放在 Docker；验收和日常运营统一用 Compose，避免“开发可用、部署不可用”。

### 13.2 资源与安全

- Docker 内存建议限制在 6–8 GB 内，避免影响 Cursor 与浏览器。
- PostgreSQL、n8n 必须使用持久化 volume。
- API Key、Cookie、内部密钥不得提交 Git；仓库只提供 `.env.example`。
- 容器内进程监听 `0.0.0.0`，否则同一 Compose 网络内的其他服务无法访问；访问范围的限制由 Compose 端口映射承担，一律写成 `127.0.0.1:8000:8000` 这种形式，不暴露到局域网或公网。
- 定期备份数据库、n8n 数据与导出目录；发布 Cookie 单独加密或由发布组件管理。

### 13.3 最小备份对象

- PostgreSQL 逻辑备份。
- n8n workflows/credentials 的受保护导出或数据卷备份。
- `data/exports`、`data/assets`。
- `.env` 的离线安全副本（不纳入普通备份包）。

## 14. 安全、合规与平台风险

- 模型与搜索调用可能把用户输入发送给第三方，页面需明确提示敏感信息不要录入。
- 引用来源需保留链接和访问时间，不能把抓取内容大段复制进公开内容。
- 涉及医疗、金融、法律等高风险主题时，必须额外人工核查或禁止自动发布。
- 平台发布优先官方 API；浏览器自动化容易受页面改版、登录态、验证码和风控影响。
- 不建设规避风控、批量养号或绕过平台规则的能力。
- “点击成功”不等于发布成功；必须通过平台内容 ID、链接或页面回查验证。
- Prompt injection：外部网页内容只作为不可信资料，不能作为系统指令执行；抽取时应隔离与清洗。
- 日志不得记录完整 API Key、Cookie、Authorization Header 或敏感原文。

## 15. 主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 外部模型/搜索不可用或限流 | 任务失败或耗时增加 | 超时、有限重试、错误分类、可恢复步骤 |
| 模型输出不符合 Schema | 后续步骤无法处理 | Structured Output、Pydantic 校验、一次修复调用 |
| 事实幻觉和错误引用 | 品牌与合规风险 | 来源绑定、规则 QC、人工批准门禁 |
| n8n 与数据库状态漂移 | 页面显示错误、重复执行 | FastAPI 负责状态，幂等键与 reconciliation job |
| Windows/Docker 资源紧张 | 系统卡顿 | 模块化单体、限制并发、按需启用发布容器 |
| 发布器受页面改版影响 | 发布失败或误判 | Adapter 隔离、手工导出回退、发布后验证 |
| 密钥/Cookie 泄露 | 账号与费用风险 | 本地密钥、日志脱敏、不提交 Git、最小暴露 |
| 模型调用成本失控 | 个人自费超支，且事后才在日志里发现 | 维护模型定价配置，设任务级与日级预算；软阈值告警，硬阈值将任务置为 `needs_attention` 并停止后续调用 |
| 需求膨胀 | 无法按期完成 | P0 门禁、V1 不做清单、按验收指标推进 |
| 开源许可证不兼容 | 商业化风险 | 接入前许可证清单，AGPL 组件隔离或替换 |

## 16. 路线图

V1 按 4 个里程碑推进。这套 M1–M4 编号取代了早期方案里的「阶段 0–4」，也是《AI 内容运营控制台—V1 产品与实施设计》里里程碑编号的唯一来源，两份文档不再各用一套编号。里程碑的先后顺序体现「快速见效优先」：M1 结束时就应该有一个可以打开浏览器演示的版本，后续里程碑在这个可运行的骨架上加厚。

### M1 走通闭环（全 Mock 驱动）

- 工程底座：Monorepo、Compose、环境变量校验、数据库迁移、健康检查。
- 任务创建、列表与详情，派生总状态，幂等提交。
- n8n WF-01 主流程骨架与步骤回调。
- Mock Provider 产出内容，页面查看、批准并导出 Markdown。
- 交付：一个不依赖任何外部 API、可本地演示的端到端 demo。

### M2 真实内容生产

- 接入搜索 API 与 LLM Provider，结构化输出与 Prompt 管理。
- Research Brief、Content Brief、小红书图文与抖音脚本。
- ProviderCall 记录、成本估算、任务级与日级预算及熔断。

### M3 质检与人工审核

- 确定性规则 QC 与模型辅助 QC，一次受控修订。
- 版本管理、审核门禁、审核队列、编辑与版本冲突处理。
- Claim 与来源引用的完整性校验。

### M4 稳定性、导出与试运行

- 错误分类与重试、卡住任务对账、备份与恢复演练。
- Markdown/JSON 导出、Fake Publisher 的 Adapter 契约验证。
- E2E、Prompt 黄金样本回归、20 个真实选题试运行并采集耗时与成本基线。

### V1.1（不在 V1 范围内）

- 图片与卡片模板能力。
- 小红书、抖音真实发布 Adapter 接入。
- 发布后数据采集与选题反馈。

### 执行方式

本项目按 superpowers 流程执行。每个里程碑开工前，先在 `docs/superpowers/plans/` 下产出该里程碑的实施计划：任务级分解、每个任务先写失败测试再写实现、任务完成后过一轮代码评审。实现阶段遵循 TDD，不接受先写实现后补测试。

## 17. 决策门槛

只有满足以下条件，才进入下一层自动化：

- 内容质量达到《AI 内容运营控制台—V1 产品与实施设计》§18.2 规定的阈值与统计口径，本文件不另设数字。
- 核心事实来源检查没有严重漏项。
- 生成成本和耗时在个人可接受范围。
- 发布 Adapter 在隔离账号上连续测试达到约定成功率，并能可靠识别失败。
- 人工审核仍保留，且用户明确愿意承担平台风险。

## 18. 项目最终定位

V1 的准确定位是：

> 一套在个人 Windows 电脑本地运行的 AI 内容生产与审核控制台，通过外部大模型 API、搜索 API、MCP 和可插拔发布组件，完成从内容任务到批准稿及受控发布的可追踪闭环。

它不是复杂 SaaS，不是无人值守的账号矩阵工具，也不是通用 Agent 平台。第一阶段最重要的成果，是让用户稳定地在浏览器中完成每天的内容运营，并能清楚知道系统生成了什么、依据什么、花费多少、失败在哪里以及最终批准了哪个版本。

## 19. 修订记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-08-19 | V1.1 | 基于产品与架构评审结论调整：量化验收指标改为引用 V1 设计文档、成本护栏提升为 V1 必做、调研改用自带正文的搜索 API 且不自建抓取、发布能力收紧为导出与 Fake Adapter 契约验证、路线图重构为 M1–M4 并确立快速见效优先原则、明确保留 n8n 的理由。 |
| — | V1.0 | 初版立项方案。 |
